# ==========================================================
# Variáveis de ambiente — BACKEND (Node)
# Copie este arquivo para ".env" e preencha. NUNCA comite o .env real.
# ==========================================================

# URL do projeto Supabase
SUPABASE_URL=https://vzufbqncbawoxrptxzuw.supabase.co

# Chave service_role (Supabase → Settings → API → service_role).
# Ignora o RLS — uso EXCLUSIVO de backend. Nunca colocar no frontend.
SUPABASE_SERVICE_ROLE_KEY=cole_aqui_a_service_role_key
